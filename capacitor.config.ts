import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'weather-tutorial-demo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
